# Desativa a listagem de diretórios para maior segurança
Options -Indexes

# Ativa o motor de reescrita de URLs
RewriteEngine On

# Redireciona tudo para public/index.php, a menos que...
# ...o caminho corresponda a um diretório real no disco
RewriteCond %{REQUEST_FILENAME} !-d
# ...ou o caminho corresponda a um arquivo real no disco (como CSS, JS, imagens)
RewriteCond %{REQUEST_FILENAME} !-f
# ...então reescreva a solicitação para o index.php dentro do diretório public
RewriteRule ^ index.php [L]
